import 'package:flutter/foundation.dart';

/// Fallback stub — should never be reached at runtime.
Future<String?> saveToGallery(Uint8List bytes, String fileName) async => null;
Future<void> shareImage(Uint8List bytes, String fileName) async {}
